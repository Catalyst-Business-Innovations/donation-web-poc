export interface FraudAlert {
  id: string;
  icon: string;
  title: string;
  detail: string;
  bg: string;
  border: string;
}
export interface TierStat {
  icon: string;
  label: string;
  threshold: string;
  count: number;
  bg: string;
}
